Preface
-- Category: The Essence of Composition
A Category consists of objects and arrows between them. 
The arrows can be composed and must obey certain laws:
 composition must be associative and there must be an identity arrow for each object.

> import Prelude
> -- This is a Haskell code block
> -- | Computes the square of an integer.
> -- Takes an integer 'x' and returns 'x' squared.
> square :: Int -> Int
> square x = x * x

-- Composition is the Essence of Programming
